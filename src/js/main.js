;
(() => {
    'use strict';

    /*variables*/

    /*BEGIN PARALLAX*/
    const Parallax = require('scroll-parallax');
    let p = new Parallax('.parallax', {
        intensity:80
    }).init();

    /*END PARALLAX*/

})();
